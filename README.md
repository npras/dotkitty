kitty terminal config

```
mkdir -p ~/.dotfiles/dotkitty
mkdir -p ~/.config/kitty

git clone git@github.com:npras/dotkitty.git
cd dotkitty

ln -sf ~/.dotfiles/dotkitty/kitty.conf ~/.config/kitty/kitty.conf
```
